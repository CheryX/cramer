---
title: Example Post
description: This frontmatter description will appear below the title
tags: [a, b]
thumbnail: 'https://images.unsplash.com/photo-1635775017492-1eb935a082a4?ixlib=rb-1.2.1&ixid=MnwxMjA3fDB8MHxwaG90by1wYWdlfHx8fGVufDB8fHx8&auto=format&fit=crop&w=1000&q=80'
---

This is an example post, with a [link](https://nextjs.org) and a React component:

<Winogrona />

```js
    var ctx = await utils.getObject(client, interaction)
    var commandArgs = utils.getArgs(interaction)

    if (commandArgs.user == undefined) { commandArgs.user = ctx.user.id }
    var gctx = await utils.fetchMember(client, interaction, commandArgs.user)

    var avatarURL = gctx.user.avatarURL({format: 'png', dynamic: true, size: 1024}) || 'https://cdn.discordapp.com/embed/avatars/0.png';
    var bannerURL = `https://cdn.discordapp.com/banners/${gctx.user.id}/${gctx.rawuser.banner}?size=512`
    var createdAt = gctx.user.createdAt

    var iconColor = await util.getDominantColor(avatarURL);
    var userColor = util.rgbToHex(iconColor.Vibrant.rgb);
```

<Graph fun="x**2" />

<Graph fun="-(x**2)" />

<Graph fun="Math.sin(x)" />

<Graph fun="Math.tan(x)" />

<Graph fun="1/x" />

<Graph fun="Math.sqrt(4-x**2)" />

<Graph fun="(x+2)*(x+1)*x*(x-1)*(x-2)" />

<AccordionGroup>
    <Accordion title="hejnał">jesteś gwiazdą</Accordion>
    <Accordion title="Wenomenaindesama">Wifelinloof</Accordion>
    <Accordion title='Masz tu przyklad zlego kodu'>
        ```js
        // Przykladowy kod tobiasa
        if (a) {
            a = a;
        }
        ```
    </Accordion>
    <Accordion title="a little trolling"></Accordion>
</AccordionGroup>

The title and description are pulled from the MDX file and processed using `gray-matter`. Additionally, links are rendered using a custom component passed to `next-mdx-remote`.

```cpp
#include <iostream>
using namespace std;

int main() {
    cout << "Hello World!" << endl;
    return 0;
}
```

$$
x^2 + y^2 = z^2
$$

Go back [home](/).

<AccordionGroup>
    <Accordion title="hejnał">jesteś gwiazdą</Accordion>
    <Accordion title="Wenomenaindesama">Wifelinloof</Accordion>
    <Accordion title='Masz tu przyklad zlego kodu'>
        ```js
        // Przykladowy kod tobiasa
        if (a) {
            a = a;
        }
        ```
    </Accordion>
    <Accordion title="a little trolling"></Accordion>
</AccordionGroup>

# GFM

## Autolink literals

www.example.com, https://example.com, and contact@example.com.

## Footnote

A note[^1]

[^1]: Big note.

## Strikethrough

~one~ or ~~two~~ tildes.

## Table

| a | b  |  c |  d  |
| - | :- | -: | :-: |

## Tasklist

* [ ] to do
* [x] done

| Alpha    | Bravo   |
| -------- | ------- |
| 中文       | Charlie |
| 👩‍❤️‍👩 | Delta   |